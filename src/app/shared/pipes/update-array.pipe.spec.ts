import { UpdateArrayPipe } from './update-array.pipe';

describe('UpdateArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new UpdateArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
